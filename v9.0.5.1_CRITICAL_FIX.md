# v9.0.5.1 긴급 수정 가이드

## 🚨 문제 발견 및 해결

### 📋 발견된 문제

#### 문제 1: 선생님 등록 후 사라짐
**증상**:
- 권미정 선생님 등록 완료
- 페이지 새로고침 (`F5`)
- 권미정 선생님이 목록에서 사라짐

**원인**:
- `js/members.js`의 `quickAddTeacher()` 함수에서 `username`, `password`, `role` 필드를 DB에 저장하지 않음
- Supabase `teachers` 테이블에 해당 컬럼이 존재하지 않음

**코드 문제** (Line 2448-2460):
```javascript
// 변경 전 (잘못된 코드)
const newTeacher = {
    name: name,
    phone: phone,
    work_hours: workHours,
    status: '재직',
    subject: '수학',
    memo: ''
};
console.warn('경고: role, username, password는 DB에 저장되지 않습니다.');
```

#### 문제 2: 전체 회원관리 페이지 오류
**증상**:
- 설하령 선생님으로 로그인
- 전체 회원관리 페이지 접근
- "데이터를 불러오는데 실패했습니다" 오류 표시

**원인**:
- `js/permissions.js`의 `canEditStudent()`, `canDeleteStudent()`, `canEditAttendance()` 함수에서 `allStudents` 전역 변수가 `undefined`일 때 처리하지 않음
- 선생님 로그인 시 `allStudents`가 로드되기 전에 권한 체크 함수 호출

---

## 🛠️ 해결 방법

### ✅ 해결 1: Supabase 데이터베이스 마이그레이션

**필수 작업**: Supabase에서 다음 컬럼을 추가해야 합니다.

#### Supabase 대시보드 접속
1. https://app.supabase.com 접속
2. 프로젝트 선택: **MATHPOCKET**
3. **SQL Editor** 클릭
4. **New Query** 클릭

#### SQL 실행
다음 SQL을 복사하여 실행:

```sql
-- teachers 테이블에 로그인 인증 컬럼 추가
ALTER TABLE teachers ADD COLUMN IF NOT EXISTS username TEXT UNIQUE;
ALTER TABLE teachers ADD COLUMN IF NOT EXISTS password TEXT;
ALTER TABLE teachers ADD COLUMN IF NOT EXISTS role TEXT CHECK (role IN ('teacher', 'subadmin')) DEFAULT 'teacher';
ALTER TABLE teachers ADD COLUMN IF NOT EXISTS work_hours TEXT;
ALTER TABLE teachers ADD COLUMN IF NOT EXISTS subject TEXT;
ALTER TABLE teachers ADD COLUMN IF NOT EXISTS memo TEXT;

-- students 테이블에 담당 선생님 매핑 컬럼 추가
ALTER TABLE students ADD COLUMN IF NOT EXISTS teacher_id UUID REFERENCES teachers(id) ON DELETE SET NULL;

-- 인덱스 생성 (성능 향상)
CREATE INDEX IF NOT EXISTS idx_teachers_username ON teachers(username);
CREATE INDEX IF NOT EXISTS idx_teachers_role ON teachers(role);
CREATE INDEX IF NOT EXISTS idx_students_teacher_id ON students(teacher_id);

-- 기존 선생님 데이터에 role 기본값 설정
UPDATE teachers SET role = 'teacher' WHERE role IS NULL;
```

**Run 버튼 클릭** (또는 `Ctrl + Enter`)

#### 실행 결과 확인
성공 메시지가 표시되면 완료:
```
Success. No rows returned
```

#### 테이블 구조 확인
1. **Table Editor** 클릭
2. **teachers** 테이블 선택
3. 새 컬럼 확인:
   - ✅ username (로그인 아이디)
   - ✅ password (비밀번호)
   - ✅ role (역할: teacher, subadmin)
   - ✅ work_hours (근무시간)
   - ✅ subject (담당 과목)
   - ✅ memo (메모)

4. **students** 테이블 선택
5. 새 컬럼 확인:
   - ✅ teacher_id (담당 선생님 ID)

---

### ✅ 해결 2: 코드 수정

#### 수정 1: `js/members.js` (Line 2448-2460)
**변경 내용**: `quickAddTeacher()` 함수에 username, password, role 저장

```javascript
// 변경 후 (올바른 코드)
const newTeacher = {
    name: name,
    phone: phone,
    work_hours: workHours,
    status: '재직',
    subject: '수학',
    memo: '',
    role: role,           // ✅ 추가
    username: username,   // ✅ 추가
    password: password    // ✅ 추가
};

console.log('새 선생님 등록:', newTeacher);
await API.create('teachers', newTeacher);
```

#### 수정 2: `js/permissions.js` (Line 60-65, 74-79, 90-95)
**변경 내용**: `allStudents` undefined 체크 추가

```javascript
// canEditStudent 함수
if (Auth.isTeacher()) {
    if (typeof allStudents === 'undefined' || !allStudents) {
        console.warn('[Permissions] allStudents가 정의되지 않음');
        return false;
    }
    const student = allStudents.find(s => s.id === studentId);
    return student && student.teacher_id === Auth.getUserId();
}
```

동일한 수정을 `canDeleteStudent()`, `canEditAttendance()`에도 적용

#### 수정 3: `js/members.js` (Line 2810-2813)
**변경 내용**: `loadAllMembers()` 오류 로깅 추가

```javascript
} catch (error) {
    console.error('[loadAllMembers] 오류 발생:', error);  // ✅ 추가
    document.getElementById('membersByGrade').innerHTML = 
        '<p class="text-center">데이터를 불러오는데 실패했습니다</p>';
}
```

---

## 📦 배포 절차

### 1단계: Supabase 마이그레이션 (필수)
⚠️ **이 단계를 먼저 완료해야 합니다!**

위의 SQL을 Supabase 대시보드에서 실행

### 2단계: GitHub 업로드
```bash
git add js/members.js
git add js/permissions.js
git add supabase-schema.sql
git add supabase-migration-teachers-auth.sql
git add SUPABASE_MIGRATION_GUIDE.md
git add v9.0.5.1_CRITICAL_FIX.md
git add README.md

git commit -m "v9.0.5.1 - 긴급 수정: 선생님 등록/로그인 기능 및 권한 체크 안정성 개선"

git push origin main
```

### 3단계: Netlify 자동 재배포
- GitHub 연동 시 자동으로 재배포됨
- 배포 완료까지 약 1-2분 소요

### 4단계: 브라우저 캐시 삭제
- **Windows/Linux**: `Ctrl + Shift + R`
- **Mac**: `Cmd + Shift + R`

---

## ✅ 배포 후 테스트

### 테스트 1: 선생님 등록 및 유지 확인
1. 관리자로 로그인
2. **회원 관리** → **선생님 관리**
3. 새 선생님 등록:
   - 이름: 권미정
   - 역할: 선생님
   - 전화번호: 010-1234-5678
   - 근무시간: 월~금 14:00-18:00
   - 아이디: kwonmj
   - 비밀번호: 1234
4. **등록** 버튼 클릭
5. ✅ "선생님이 등록되었습니다" 알림 확인
6. **페이지 새로고침** (`F5`)
7. ✅ 권미정 선생님이 목록에 남아있는지 확인

### 테스트 2: 선생님 로그인
1. 로그아웃
2. 선생님 계정으로 로그인:
   - 아이디: kwonmj
   - 비밀번호: 1234
3. ✅ "로그인 성공" 확인
4. ✅ 선생님용 메뉴만 표시되는지 확인

### 테스트 3: 전체 회원관리 페이지
1. 설하령 선생님으로 로그인
2. **회원 관리** → **전체회원관리**
3. ✅ "데이터를 불러오는데 실패했습니다" 오류가 표시되지 않음
4. ✅ 담당 학생 목록이 학년별로 정상 표시됨
5. ✅ 인쇄 버튼 클릭 → 정상 인쇄 가능

### 테스트 4: 담당학생 매핑
1. 관리자로 로그인
2. **회원 관리** → **학생 관리**
3. 학생 선택 후 **수정**
4. **담당 선생님** 드롭다운에서 "권미정" 선택
5. **저장**
6. 로그아웃 후 권미정 선생님으로 로그인
7. ✅ 담당 학생이 모든 페이지에 표시되는지 확인

---

## 🔍 문제 해결

### 문제: "column already exists" 오류
**원인**: 컬럼이 이미 존재함  
**해결**: 정상입니다. `IF NOT EXISTS`로 안전하게 무시됨

### 문제: 선생님 등록 후에도 사라짐
**원인**: Supabase 마이그레이션을 실행하지 않음  
**해결**: 위의 SQL을 Supabase에서 실행

### 문제: "데이터를 불러오는데 실패했습니다"
**원인**: 
1. 브라우저 캐시가 남아있음
2. 코드가 최신 버전이 아님

**해결**:
1. `Ctrl + Shift + R`로 강력 새로고침
2. F12 → Console 탭에서 오류 메시지 확인
3. `[loadAllMembers] 오류 발생:` 로그 확인

---

## 📊 변경 사항 요약

### 수정된 파일
| 파일 | 변경 내용 | 라인 |
|------|-----------|------|
| `js/members.js` | quickAddTeacher() - username, password, role 저장 | 2448-2460 |
| `js/members.js` | loadAllMembers() - 오류 로깅 추가 | 2810-2813 |
| `js/permissions.js` | canEditStudent() - allStudents 체크 | 60-65 |
| `js/permissions.js` | canDeleteStudent() - allStudents 체크 | 74-79 |
| `js/permissions.js` | canEditAttendance() - allStudents 체크 | 90-95 |

### 새로 생성된 파일
- `supabase-migration-teachers-auth.sql` - 마이그레이션 SQL 스크립트
- `SUPABASE_MIGRATION_GUIDE.md` - 상세 마이그레이션 가이드
- `v9.0.5.1_CRITICAL_FIX.md` - 이 문서

### 업데이트된 파일
- `supabase-schema.sql` - teachers, students 테이블 스키마 업데이트
- `README.md` - v9.0.5.1 긴급 수정 섹션 추가

---

## 🎉 완료

v9.0.5.1 긴급 수정이 완료되었습니다!

**핵심 변경사항**:
1. ✅ 선생님 등록 시 username, password, role이 DB에 저장됨
2. ✅ 페이지 새로고침 후에도 선생님 정보가 유지됨
3. ✅ 전체 회원관리 페이지 오류 해결
4. ✅ 담당학생 필터링 정상 작동

**다음 단계**:
1. Supabase 마이그레이션 실행 ⚠️ **필수**
2. 수정된 파일 GitHub 업로드
3. Netlify 자동 재배포
4. 기능 테스트

---

## 📝 주의사항

⚠️ **보안 경고**: 현재 비밀번호를 **평문**으로 저장하고 있습니다.

**프로토타입용**이며, 프로덕션 환경에서는 다음이 필요합니다:
1. 비밀번호 해시화 (bcrypt, Argon2)
2. HTTPS 적용
3. JWT 토큰 인증
4. Rate limiting
5. SQL Injection 방어
6. XSS 방어
